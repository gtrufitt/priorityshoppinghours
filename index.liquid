<!doctype html>

<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>Priority Shopping Hours</title>
  <meta name="description" content="Supermarket priority shopping hours">


  <style>
  {% include ./reset.css %}
  {% include ./index.css %}
  </style>


</head>

<body>

    <h1 style="line-height: 0.8em;">UK Priority Shopping Hours <span style="font-size: 0.5em; display: inline-block;">during Covid-19 outbreak</span></h1>

    <p class="small">Click to view:</p>
    {% for supermarket in index.supermarkets %}
    <a href="#{{ supermarket.name | url_encode }}" class="button">
        <img style="width: 70px;" src="https://aoxpqaidqo.cloudimg.io/v7/{{supermarket.logo}}?func=fit&h=50&w=140&trim=20" alt="View details for {{ supermarket.name }}">
    </a>
    {% endfor %}
    

    <ul>
        {% for supermarket in index.supermarkets %}
            <li class="supermarket" id="{{ supermarket.name | url_encode }}">
                <div class="header">                    
                    <img class="logo" src="https://aoxpqaidqo.cloudimg.io/v7/{{supermarket.logo}}?func=fit&h=100&w=300&trim=20" alt="{{ supermarket.name }}">
                    <h2 class="sr">{{ supermarket.name }}</h2>
                </div>
                <dl class="groups">
                    {% for group in supermarket.hours %}
                        <dt><h3>{{group.title}}</h3><dt>
                        <dd><p>{{group.description}}</p><dd>
                    {% endfor %}
                </dl>
                <h4 class="small">Notes</h4>
                <p class="notes small">{{supermarket.notes}}</p>
                <a class="notes small" href="{{supermarket.detailsLink}}">View Website</a> <a class="notes small" href="{{supermarket.twitterLInk}}">View Twitter</a>
            </li>
        {% endfor %}
    </ul>

    <h3>Issues or admendments?</h3>
    <form name="contact" method="POST" data-netlify="true" netlify-honeypot="bot-field" class="small">
        <p class="hidden">
            <label>Don’t fill this out if you're human: <input name="bot-field" /></label>
        </p>
        <p>
            <label>Message: <textarea name="message" style="display: block; width: 100%; margin-bottom: 5px;"></textarea></label>
        </p>
        <p>
            <button type="submit">Send</button>
        </p>
    </form>

    <p class="small"><a href="https://github.com/gtrufitt/priorityshoppinghours">Contribute on GitHub</a></p>

  <script src="scripts.js"></script>
</body>
</html>